name: CI Adapters

# CTE I/O-adapter build+test coverage across Linux, macOS and Windows.
#
# The mainline CI (ci-linux / ci-macos / ci-windows) builds the runtime with
# every adapter OFF (ELF and FUSE are disabled everywhere). This workflow is
# the dedicated matrix that actually compiles and exercises the adapters:
#
#   * posix / stdio / vfd  -- built on the glibc ELF/dlsym interceptor
#     (real_api.h uses link.h + gelf.h), so they are Linux-only. No macOS or
#     Windows port exists.
#   * hdf5_vol             -- portable HDF5 VOL connector; Linux/macOS/Windows.
#   * libfuse (clio_cte_fuse) -- libfuse3 on Linux, macFUSE on macOS, WinFsp
#     on Windows. The Windows job is what verifies the _WIN32 WinFsp path
#     (issue #474 / feat/winscp integration).
#
# Matrix (per the scoping decision; FUSE-on-every-OS is #903):
#   Linux   : posix + stdio + vfd + hdf5_vol + libfuse  (+ live mount smoke
#             + xfstests conformance regression gate; see issue #677)
#   macOS   : hdf5_vol + libfuse (macFUSE 5 build + unit + in-process ops
#             suites; FSKit-backend mount smoke as a non-blocking probe)
#   Windows : hdf5_vol + libfuse (WinFsp build + unit + in-process ops
#             suites + live WinFsp mount smoke)

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]
  workflow_dispatch:

concurrency:
  group: ci-adapters-${{ github.ref }}
  # Cancel superseded runs on PR branches only. On a SHARED branch (main/dev)
  # github.ref is identical for every merge, so `true` here made each merge
  # cancel the PREVIOUS merge's validation mid-flight -- which is exactly the
  # "lots of skipped tests after merging" symptom, and it silently drops the
  # coverage that decides what is on dev. A merge's run must always finish.
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  # ---- Linux: all five adapters (inside the reproducible deps-cpu image) -----
  # Mirrors the boost-docker job: deps-cpu carries HDF5 >= 1.14 + libelf; we
  # add libfuse3-dev for the FUSE backend. --device /dev/fuse + SYS_ADMIN let
  # the live mount smoke test actually mount.
  linux-adapters:
    name: adapters (linux, all 5)
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          submodules: recursive
      - name: Build adapters + run adapter tests (inside deps-cpu)
        run: |
          # The script lives in CI/adapters_linux.sh rather than inline here.
          # An inline single-quoted shell body is one apostrophe away from
          # silently ending early -- which is exactly what happened here, and it
          # cost three CI rounds misdiagnosed as a deps-cpu image regression.
          # See the header of that script.
          #
          # --shm-size: docker's default /dev/shm is 64 MB; the clio runtime
          # places its GB-scale SHM segments there, so give it room (also lets
          # the FUSE runtime breathe under the xfstests gate).
          docker run --rm --shm-size=2g \
            -v "${{ github.workspace }}":/workspace -w /workspace -u 0 \
            --device /dev/fuse --cap-add SYS_ADMIN --security-opt apparmor=unconfined \
            iowarp/deps-cpu:latest bash /workspace/CI/adapters_linux.sh

  # ---- macOS: hdf5_vol + libfuse (macFUSE) -----------------------------------
  # fuse_cte.cc carries the Darwin path (fuse_darwin_attr translation, darwin
  # fill-dir type) and macFUSE 5 ships libfuse3, so the adapter now builds and
  # runs its unit + in-process ops suites here (#903) — none of that needs the
  # kext. Actually MOUNTING is the hard part on CI: the macFUSE kext requires
  # Recovery-Mode user approval (impossible on runners), so the mount smoke
  # below uses the FSKit backend (macFUSE 5.1+, macOS 15.4+, mountpoint under
  # /Volumes) and is NON-BLOCKING until proven on runner images.
  macos-adapters:
    name: adapters (macos, hdf5_vol + macfuse)
    # Push-only (#921): the linux job above covers all five adapters on
    # every PR; these two re-verify the platform-specific mount layers
    # (macfuse / winfsp) at the merge point.
    if: github.event_name != 'pull_request'
    runs-on: macos-15
    timeout-minutes: 120
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          submodules: recursive
      - name: Install dependencies (conda HDF5 provides >= 1.14)
        run: |
          chmod +x CI/ci-deps.sh
          ./CI/ci-deps.sh --only-deps release
      # macFUSE 5 ships libfuse3 (headers + dylib + pkg-config) under
      # /usr/local; installing the cask needs no kext approval — only
      # mounting through the kext backend would.
      - name: Install macFUSE (libfuse3 for the adapter build)
        run: brew install --cask macfuse
      - name: Configure and build (hdf5_vol + macfuse)
        run: |
          set -x
          if [ -f "$HOME/miniconda3/etc/profile.d/conda.sh" ]; then
            source "$HOME/miniconda3/etc/profile.d/conda.sh"
          elif [ -f "$HOME/anaconda3/etc/profile.d/conda.sh" ]; then
            source "$HOME/anaconda3/etc/profile.d/conda.sh"
          elif command -v conda >/dev/null 2>&1; then
            eval "$(conda shell.bash hook)"
          fi
          conda activate iowarp
          export CMAKE_PREFIX_PATH="${CONDA_PREFIX}${CMAKE_PREFIX_PATH:+:$CMAKE_PREFIX_PATH}"
          export PKG_CONFIG_PATH="${CONDA_PREFIX}/lib/pkgconfig:${CONDA_PREFIX}/share/pkgconfig:/usr/local/lib/pkgconfig:/opt/homebrew/lib/pkgconfig${PKG_CONFIG_PATH:+:$PKG_CONFIG_PATH}"
          cmake -B build-adapters \
            -DCMAKE_BUILD_TYPE=Debug \
            -DPython3_EXECUTABLE="${CONDA_PREFIX}/bin/python3" \
            -DSITE="macos-15" \
            -DBUILDNAME="adapters/cnd/d" \
            -DCLIO_CORE_ENABLE_RUNTIME=ON \
            -DCLIO_CORE_ENABLE_CTE=ON \
            -DCLIO_CORE_ENABLE_CAE=OFF \
            -DCLIO_CORE_ENABLE_CEE=OFF \
            -DCLIO_CORE_ENABLE_TESTS=ON \
            -DCLIO_CORE_ENABLE_BENCHMARKS=OFF \
            -DCLIO_CORE_ENABLE_PYTHON=OFF \
            -DCLIO_CORE_ENABLE_CONDA=ON \
            -DCLIO_CORE_ENABLE_ELF=OFF \
            -DCLIO_CTE_ENABLE_HDF5_VOL=ON \
            -DCLIO_CTE_ENABLE_FUSE_ADAPTER=ON \
            -DCLIO_CTE_ENABLE_ADIOS2_ADAPTER=OFF \
            -DCLIO_CTE_ENABLE_COMPRESS=OFF \
            -DCLIO_CORE_ENABLE_GRAY_SCOTT=OFF \
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON \
            -DCLIO_CTP_LOG_LEVEL=0
          cmake --build build-adapters -- -j"$(sysctl -n hw.ncpu)"
      - name: Verify the macFUSE adapter binary was built
        run: |
          if [ ! -x build-adapters/bin/clio_cte_fuse ]; then
            echo "::error::clio_cte_fuse was not built — macFUSE/libfuse3 was not detected at configure time (check fuse3 pkg-config visibility under /usr/local/lib/pkgconfig)."
            exit 1
          fi
          echo "OK: clio_cte_fuse present (macFUSE libfuse3 path compiled and linked)"
      - name: Run adapter tests
        run: |
          if [ -f "$HOME/miniconda3/etc/profile.d/conda.sh" ]; then
            source "$HOME/miniconda3/etc/profile.d/conda.sh"
          elif [ -f "$HOME/anaconda3/etc/profile.d/conda.sh" ]; then
            source "$HOME/anaconda3/etc/profile.d/conda.sh"
          elif command -v conda >/dev/null 2>&1; then
            eval "$(conda shell.bash hook)"
          fi
          conda activate iowarp
          # h5py for the VOL compat suite, source-built against the conda HDF5
          # the VOL links (a wheel's bundled libhdf5 can't load the connector).
          HDF5_DIR="$CONDA_PREFIX" "${CONDA_PREFIX}/bin/python3" -m pip install \
            --no-binary=h5py h5py numpy
          ctest --test-dir build-adapters --output-on-failure \
            --timeout 180 --repeat until-pass:2 \
            -R "hdf5_vol|fuse" -E "copy_workspace" -LE "docker|ollama"
      # Real mount attempt via the FSKit backend (kext-free; macFUSE 5.1+,
      # macOS 15.4+, mountpoint must live under /Volumes). NON-BLOCKING: FSKit
      # extension approval semantics on runner images are unproven — promote
      # to a hard gate once this step is observed green. See #903.
      # timeout + continue-on-error: the first probe HUNG for 50+ minutes
      # (fuse daemon stuck in an uninterruptible mount, the script's cleanup
      # wait blocked behind it) — `|| echo` inside the script only survives
      # failures, not hangs, so the non-blocking contract needs both a step
      # bound and continue-on-error to hold.
      - name: FUSE mount smoke (FSKit backend, non-blocking probe)
        timeout-minutes: 10
        continue-on-error: true
        run: |
          if [ -f "$HOME/miniconda3/etc/profile.d/conda.sh" ]; then
            source "$HOME/miniconda3/etc/profile.d/conda.sh"
          elif command -v conda >/dev/null 2>&1; then
            eval "$(conda shell.bash hook)"
          fi
          conda activate iowarp || true
          sudo mkdir -p /Volumes/cte_fuse_smoke
          sudo chown "$(whoami)" /Volumes/cte_fuse_smoke
          chmod +x CI/fuse_mount_smoke.sh
          CLIO_SMOKE_MOUNT_POINT=/Volumes/cte_fuse_smoke \
          CLIO_SMOKE_FUSE_OPTS="-o backend=fskit" \
            CI/fuse_mount_smoke.sh build-adapters \
            || echo "::warning::macOS FSKit mount smoke failed — non-blocking probe (kext needs Recovery-Mode approval on runners; FSKit backend unproven there). Unit + ops suites above are the enforced macOS signal. See #903."

  # ---- Windows: hdf5_vol + libfuse via WinFsp (build + unit + ops + mount) ---
  # This is the job that verifies the _WIN32 WinFsp FUSE path (issue #474):
  # mainline ci-windows builds with CLIO_CTE_ENABLE_FUSE_ADAPTER=OFF, so the
  # WinFsp compat shim (fuse_win_compat.h) is not compiled anywhere else.
  # #903 added the in-process ops suite (test_fuse_ops now builds here — its
  # old pkg-config gate never matched WinFsp) and the live mount smoke below.
  windows-adapters:
    name: adapters (windows, hdf5_vol + winfsp)
    # Push-only (#921): the linux job above covers all five adapters on
    # every PR; these two re-verify the platform-specific mount layers
    # (macfuse / winfsp) at the merge point.
    if: github.event_name != 'pull_request'
    runs-on: windows-2025
    timeout-minutes: 120
    env:
      VCPKG_DEFAULT_TRIPLET: x64-windows
      CAE_LABEL_TEST_SKIP: "1"
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          submodules: recursive
      - name: Install WinFsp (FUSE backend + dev headers/libs)
        shell: pwsh
        run: |
          $ver = "2.0.23075"
          $url = "https://github.com/winfsp/winfsp/releases/download/v2.0/winfsp-$ver.msi"
          $msi = "$env:RUNNER_TEMP\winfsp.msi"
          Write-Host "Downloading WinFsp $ver from $url"
          Invoke-WebRequest -Uri $url -OutFile $msi
          # ADDLOCAL=ALL pulls in the Developer feature (inc/fuse3 + lib).
          $p = Start-Process msiexec.exe -ArgumentList "/i `"$msi`" /qn ADDLOCAL=ALL" -Wait -PassThru
          if ($p.ExitCode -ne 0) { throw "WinFsp install failed ($($p.ExitCode))" }
          Get-ChildItem "C:\Program Files (x86)\WinFsp\inc\fuse3" -ErrorAction SilentlyContinue
      - name: Bootstrap vcpkg
        run: |
          cd $env:VCPKG_INSTALLATION_ROOT
          git pull
          .\bootstrap-vcpkg.bat
      - name: Configure CMake (hdf5_vol + WinFsp fuse)
        # VCPKG_APPLOCAL_DEPS=OFF disables vcpkg's per-target post-build
        # "z-applocal" DLL copy into build/bin/ altogether. With HDF5_VOL=ON
        # this job links hdf5/szip/zstd/zmq into many parallel test-exe
        # targets, and the copy step intermittently died with
        # `error MSB3073: vcpkg z-applocal ... exited with code 32`
        # (ERROR_SHARING_VIOLATION) -- even AFTER
        # X_VCPKG_APPLOCAL_DEPS_SERIALIZED=ON serialized the copies against
        # each other (2026-07-31, twice in one day: something else, likely
        # Defender scanning the freshly linked exe, still held the file).
        # Instead of copying, the test step below puts the vcpkg installed
        # bin dir on PATH so the exes resolve those DLLs in place.
        # ci-windows.yml doesn't hit this because it builds HDF5_VOL=OFF
        # (far fewer shared DLLs). See issue #848 (flake log).
        run: |
          cmake -B build -A x64 `
            -DCMAKE_TOOLCHAIN_FILE="$env:VCPKG_INSTALLATION_ROOT/scripts/buildsystems/vcpkg.cmake" `
            -DVCPKG_TARGET_TRIPLET="x64-windows" `
            -DVCPKG_MANIFEST_DIR="${{ github.workspace }}/installers/vcpkg" `
            -DVCPKG_OVERLAY_PORTS="${{ github.workspace }}/installers/vcpkg/overlay-ports" `
            -DVCPKG_APPLOCAL_DEPS=OFF `
            -DCMAKE_BUILD_TYPE=Debug `
            -DSITE="windows-2025" `
            -DBUILDNAME="adapters/msvc/x64/d" `
            -DCLIO_CORE_ENABLE_RUNTIME=ON `
            -DCLIO_CORE_ENABLE_CTE=ON `
            -DCLIO_CORE_ENABLE_CAE=OFF `
            -DCLIO_CORE_ENABLE_CEE=OFF `
            -DCLIO_CORE_ENABLE_TESTS=ON `
            -DCLIO_CORE_ENABLE_PYTHON=OFF `
            -DCLIO_CORE_ENABLE_ZMQ=ON `
            -DCLIO_CORE_ENABLE_CEREAL=ON `
            -DCLIO_CORE_ENABLE_CONDA=OFF `
            -DCLIO_CORE_ENABLE_ELF=OFF `
            -DCLIO_CORE_ENABLE_RPATH=OFF `
            -DCLIO_CTE_ENABLE_HDF5_VOL=ON `
            -DCLIO_CTE_ENABLE_FUSE_ADAPTER=ON `
            -DCLIO_CTE_ENABLE_VFD=OFF `
            -DCLIO_CORE_ENABLE_BENCHMARKS=OFF `
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON `
            -DCLIO_CTP_LOG_LEVEL=0
      - name: Build
        run: cmake --build build --config Debug -j $env:NUMBER_OF_PROCESSORS
      - name: Verify the WinFsp FUSE binary was built
        shell: pwsh
        run: |
          $exe = "build/bin/clio_cte_fuse.exe"
          if (-not (Test-Path $exe)) {
            throw "clio_cte_fuse.exe was not built -- WinFsp was not linked (check the WINFSP_ROOT discovery in adapter/libfuse/CMakeLists.txt)."
          }
          Write-Host "OK: $exe present (WinFsp FUSE path compiled and linked)"
      - name: Run adapter tests
        # The Python VOL compat suite (cte_hdf5_vol_compat_suite) needs h5py to
        # load the connector, which requires h5py built against the SAME hdf5.dll
        # as the VOL — impractical on Windows (debug/release CRT split + DLL
        # discovery). The MSVC-built C++ VOL unit tests (cte_hdf5_vol_*) above are
        # the Windows VOL coverage; exclude the compat suite here.
        run: |
          # VCPKG_APPLOCAL_DEPS=OFF above means dependency DLLs were never
          # copied next to the exes -- resolve them from the vcpkg tree.
          $env:PATH = "${{ github.workspace }}\build\vcpkg_installed\x64-windows\debug\bin;${{ github.workspace }}\build\vcpkg_installed\x64-windows\bin;$env:PATH"
          # fuse_ops is excluded on Windows: it hangs with zero output until
          # the ctest timeout (at 300s AND 900s ceilings — hung, not slow).
          # Tracked in #907; test_fuse_adapter + the live mount smoke below
          # are the Windows FUSE coverage until it is root-caused.
          ctest --test-dir build --build-config Debug --output-on-failure --timeout 120 -R "hdf5_vol|fuse" -E "copy_workspace|compat_suite|fuse_ops" -LE "docker|ollama"
      # Live end-to-end WinFsp mount (#903): runtime up -> compose -> mount a
      # drive letter -> write/read/verify -> unmount. The WinFsp MSI install
      # above included the driver, so mounting works without a reboot.
      # timeout-minutes: a wedged runtime/mount must not burn the 120m job.
      - name: Live WinFsp mount smoke
        timeout-minutes: 15
        shell: pwsh
        run: |
          $env:PATH = "${{ github.workspace }}\build\vcpkg_installed\x64-windows\debug\bin;${{ github.workspace }}\build\vcpkg_installed\x64-windows\bin;$env:PATH"
          pwsh -File CI/fuse_mount_smoke.ps1 -BuildDir build
